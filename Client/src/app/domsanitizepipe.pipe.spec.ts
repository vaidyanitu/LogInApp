import { DomsanitizepipePipe } from './domsanitizepipe.pipe';

describe('DomsanitizepipePipe', () => {
  it('create an instance', () => {
    const pipe = new DomsanitizepipePipe();
    expect(pipe).toBeTruthy();
  });
});
